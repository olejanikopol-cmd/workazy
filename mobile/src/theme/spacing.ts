/** Spacing scale: 4, 8, 12, 16, 20, 24, 32. */
export const spacing = {
  xs: 4,
  sm: 8,
  md: 12,
  lg: 16,
  xl: 20,
  xxl: 24,
  xxxl: 32,
} as const;

/** Screen horizontal padding. */
export const screenPadding = 20 as const;

/** Minimum comfortable touch target. */
export const touchTarget = 44 as const;